package minijuego;

import java.awt.Color;
import javax.swing.JPanel;

public class PanelPreguntados extends JPanel {

    public void PanelBasico() {
        // Establecer el tamaño del panel
        setSize(700, 600);

        // Color de fondo del panel
        setBackground(Color.LIGHT_GRAY);

        // Layout: null para colocar componentes manualmente
        // o puedes usar GridLayout, FlowLayout, etc.
        setLayout(null);

        // Aquí luego puedes añadir botones, etiquetas, etc.
    }
}